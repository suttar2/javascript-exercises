const caesar = function(string, increment) { // lets make a function with two parameters

    string
    

};

console.log(caesar("ABCDEFGHIJKLMNOPQRSTUVWXYZ abcdefghijklmnopqrstuvwxyz", 1))

module.exports = caesar


/* First Attempt 12/09/2020


newArray = Array.from(string); // lets make an array out of the first, and store it in a variable

    newArray.forEach(convertASCII); // now lets do our new function to each item in that new array

    function convertASCII(item, index, arr) { //lets make a new function that takes 3 variables I'm not sure how it pulls these variables from the array, but lets go with it!
    arr[index] = item.charCodeAt(0); // item at index is now that items char code
    arr[index] = Number(arr[index]); // item at index is now converted to a number from a string
    arr[index] = arr[index] + increment; //item at index is now itself plus the incrementor
    arr[index] = String.fromCharCode(arr[index]); //item at index is now converted to a string based on character code
    };

    
    newString = newArray.toString(); //lets make a new variable that is our array converted to a string

    return newString; // show me the string!
*/